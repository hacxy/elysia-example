{
  "name": "elysia-app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/server.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun-darwin-arm64 --outfile server ./src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "elysia": "latest",
    "prismabox": "^1.1.25"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.19.0"
  },
  "module": "src/index.js"
}
